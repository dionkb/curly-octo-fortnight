// const validateColor = require("validate-color");

// const checkColor = (userColor) => {
//     const { color = "", text = "" } = userColor;
//     const theColor = color && validateColor(color) ? color : "transparent";
//     return theColor;
// };

// module.exports = ColorBox;